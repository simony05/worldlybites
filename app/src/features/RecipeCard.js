import React from 'react';

export const RecipeCard = ({ cuisine, image, instructions, ingredients, title }) => {
    return (
        <View>
            <Text>{title}</Text>
        </View>
    );
};